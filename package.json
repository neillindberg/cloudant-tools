{
  "name": "cloudant-backup",
  "version": "0.0.10",
  "description": "Backup Cloudant DB with pointers to environment and collection name(s)",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./src/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudant/cloudant": "^4.2.2",
    "@cloudant/couchbackup": "^2.5.0"
  }
}
